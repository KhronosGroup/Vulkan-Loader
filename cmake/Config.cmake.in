@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/@TARGETS_EXPORT_NAME@.cmake")
check_required_components(Vulkan)

include(CMakeFindDependencyMacro)
# look for Vulkan::Headers dependency
find_dependency(VulkanHeaders)

# Legacy variables, do *not* use. May be removed in the future.
set (Vulkan_INCLUDE_DIR "${VulkanHeaders_INCLUDE_DIRS}")
set (Vulkan_LIBRARY     "Vulkan::Vulkan")
set (Vulkan_LIBRARY_DIR "@PACKAGE_VK_LIBRARY_DIR@")
